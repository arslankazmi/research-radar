/**
 * OpenClaw plugin entry point — thin re-export of the plugin module.
 * OpenClaw loads the `default` export from the path listed in openclaw.extensions.
 */
export { default } from "./plugin/index.js";
